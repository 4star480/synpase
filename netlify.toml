[build]
  # Download JPG covers into public/ (gitignored locally; bundled in deploy output)
  command = "npx prisma db push && npm run images:download && npm run build"

[build.environment]
  NODE_VERSION = "20"

# Netlify auto-applies the OpenNext adapter for Next.js — do not set publish manually.
