[build]
  command = "npx prisma db push && npx tsx prisma/patch-traders.ts && npm run images:download && npm run build"

[build.environment]
  NODE_VERSION = "20"

# Netlify auto-applies the OpenNext adapter for Next.js — do not set publish manually.
